export const storageConfig = {
    STORAGE_URL : process.env.STORAGE_URL || "https://storage.googleapis.com",
    STORAGE_BUCKET : process.env.STORAGE_BUCKET || "my-bucket",
    STORAGE_API_KEY : process.env.STORAGE_API_KEY || "my-api-key",
}